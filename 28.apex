Boolean isItSunny = true;
Boolean isItRunning = false;
Boolean areYouWorking = false;

if (isItSunny == true) {
    System.debug('Go to the Party!');
        } else if (isItRunning) {
            System.debug('Go to code!');
        } else if (areYouWorking) {
            System.debug('Go to code!');
 }


